name: CI/CD ProductTrendTracker

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1 — Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2 — Set up Python
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Step 3 — Install dependencies (NOTE: explicit path!)
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r product-trend-tracker/requirements.txt
          pip install pytest pytest-cov httpx pyarrow

      # Step 4 — Run tests with coverage
      - name: Run tests
        working-directory: product-trend-tracker
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          pytest --cov=. --cov-report=xml --cov-report=term-missing
          pytest --cov=. --cov-report=html

      # Step 5 — Upload coverage report
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: product-trend-tracker/htmlcov/

      # Step 6 — Build Docker image from product-trend-tracker
      - name: Build Docker image
        if: ${{ hashFiles('product-trend-tracker/Dockerfile') != '' }}
        working-directory: product-trend-tracker
        run: |
          docker build -t product-trend-tracker:${{ github.sha }} .

      # Step 7 — Login to GHCR
      - name: Login to GHCR
        if: ${{ hashFiles('product-trend-tracker/Dockerfile') != '' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Step 8 — Push Docker image to your user namespace
      - name: Push Docker image
        if: ${{ hashFiles('product-trend-tracker/Dockerfile') != '' }}
        working-directory: product-trend-tracker
        run: |
          USER_LC="${GITHUB_ACTOR,,}"
          TAG="${{ github.sha }}"
          docker tag product-trend-tracker:$TAG ghcr.io/$USER_LC/product-trend-tracker:$TAG
          docker push ghcr.io/$USER_LC/product-trend-tracker:$TAG
